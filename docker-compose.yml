services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: eyelearn
      POSTGRES_PASSWORD: eyelearn
      POSTGRES_DB: eyelearn
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
